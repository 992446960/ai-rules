## [14:30] feat(plan): 首版 UI 说明 + Frame 设计稿

- **背景**: 计划要求「UI/产品先出一版，再通过 MCP 生成可看的一版」；需先输出想法再生成 frame 设计稿供确认。
- **变更点**:
 1. 新增 `plan/vue3-personal-hub-ui-spec-2025-02-25.md`：首版视觉与信息结构方案（路由、布局、配色、字体、主题与 i18n、关键页面 frame 要点）。
 2. 新增 `plan/frame-design-vue3-personal-hub.html`：可浏览器打开的 frame 设计稿，含首页（顶 Nav + 四大入口卡片）与学习收藏页（左侧文件夹 + 右侧列表/筛选/表格）。
- **影响**: 仅规划与设计稿，未改代码；Figma MCP 未在本次会话中成功调用（图像生成因权限失败），故以 HTML 静态稿作为可交付物。
- **自检**: 文档与 HTML 可读、可打开；确认后可按该版进入开发。

## [14:45] refactor(plan): 按 frontend-design 规范重做 Frame 设计稿

- **背景**: 用户要求以 frontend-design 规范来做；需遵循 ~/.codex/skills/frontend-design 的 SKILL.md（避免通用 AI 审美、选定美学方向、字体/配色/动效/空间/背景）。
- **变更点**:
 1. 在 `plan/vue3-personal-hub-ui-spec-2025-02-25.md` 新增「§7 frontend-design 规范」：美学方向为克制极简+编辑感；字体 Syne + DM Sans + Noto Sans SC；主色墨绿 #0f766e、强调琥珀 #d97706；stagger 动效、略不对称布局、背景微渐变+噪点。
 2. 重写 `plan/frame-design-vue3-personal-hub.html`：引入 Google Fonts（Syne/DM Sans/Noto Sans SC）、CSS 变量、卡片入场动画与 hover 上浮、背景 ::before/::after 层次与 SVG 噪点、学习收藏区统一设计语言。
- **影响**: 仅规划与静态稿，未改业务代码；设计稿 v2 与 UI 说明一致。
- **自检**: HTML 在浏览器中可正常打开并展示动效与样式。

## [HH:mm] feat(cursor-rules): 新增「从仓库安装 frontend-design 技能」规则并完成安装

- **背景**: 用户要求创建 Cursor 技能：从仓库下载 frontend-design 技能，并直接执行安装（忽略 .cursor/rule.mdc）。
- **变更点**:
 1. 新增 `cursor/rules/install-frontend-design-skill.mdc`：描述从 anthropics/skills 安装 frontend-design 的时机、安装命令（Codex skill-installer）、安装后如何在 Cursor 中使用。
 2. 执行安装：`install-skill-from-github.py --repo anthropics/skills --path skills/frontend-design`，已成功安装至 `~/.codex/skills/frontend-design`。
- **影响**: 仅新增规则文件与本地 Codex skills 目录；frontend-design 来源为 anthropics/skills（非 openai/skills）。
- **自检**: 规则文件可读；安装脚本退出码 0，目标目录已存在。

## [15:20] feat(plan): 从 Figma 设计文件提取前端设计规范摘要

- **背景**: 用户提供 Figma 链接（file key 4DHzqJpGDh3o2x3SWVi3y0, node-id 0-1），要求提取布局、配色、字体、圆角阴影边框、关键组件尺寸与间距，供另一 Agent 重写 HTML/CSS。当前会话未暴露 Figma MCP 工具，改用 Figma REST API 拉取。
- **变更点**:
 1. 使用 Figma API 拉取完整文件并保存为 `plan/figma-file-raw.json`（约 770KB）。
 2. 用 jq/Node 从 JSON 中解析：节点树与布局（layoutMode、itemSpacing、padding）、颜色（fills/backgroundColor/strokes）转 HEX、字体样式（Inter、字重/字号/行高）、cornerRadius、effects（DROP_SHADOW）、关键 FRAME 尺寸。
 3. 新增 `plan/figma-design-spec-2025-02-25.md`：设计规范摘要，含布局结构、配色表、字体表、圆角/阴影/边框、组件尺寸与间距、以及可直接复用的 CSS 变量建议。
- **影响**: 仅新增规划文档与原始 JSON；无代码变更。规范可直接交给 Agent 或开发者实现页面。
- **自检**: 规范文档结构完整、数值与 Figma 数据一致；hex/rgb 已核对。
