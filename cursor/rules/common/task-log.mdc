---
description: 项目变更日志规范 (Task Log) — 完成任务后追加 doc/logs 记录。仅在被 @task-log 引用时执行
alwaysApply: false
---

# 项目变更日志规范 (Task Log)

**本规则仅在显式引用时生效**（如 `@task-log`）。完成具体任务后，按以下规范维护变更记忆。

## 1. 日志文件

- **路径**：`doc/logs/YYYY-MM-DD.md`（例如 `doc/logs/2024-05-20.md`）
- **生成方式**：使用系统当前日期，格式化为 `YYYY-MM-DD`
- 若文件不存在，请自动创建

## 2. 日志内容格式

每次 Tool Call 完成任务后，追加以下内容：

```markdown
## [HH:mm] <Type>(<Scope>): <Subject>
- **背景**: 简述修改原因。
- **变更点**:
 1. 修改了文件 `path/to/file`
 2. 实现了 xxx 逻辑
- **影响**: 是否影响公共组件？需注意什么？
- **自检**: 已验证功能正常。
```

## 3. 目的

- 保持长期记忆，防止上下文丢失
- 方便 Review 和回溯问题

## 4. 行为约束

- 不记录无意义操作
- 日志内容必须真实、可读

## 5. 任务日志格式（5.1 对应）

每完成一个阶段性开发任务目标或修复，在 `doc/logs/YYYY-MM-DD.md` 中追加记录：

```markdown
## [时间] 任务类型 (feat/fix/refactor...)
- **背景**: 为什么要做这个修改？
- **变更点**:
 1. 修改了 X 模块 (文件路径)
 2. 新增了 Y 功能
- **影响**: (是否破坏性变更？是否影响公共组件？)
- **自检**: TS 类型检查通过，无未处理的副作用。
```
