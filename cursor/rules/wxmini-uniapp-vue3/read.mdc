---
alwaysApply: false
---
## to do

1、语音消息类型样式参考图一，点击播放时候需要加上播放效果。
2、长按录音的时候参考图2的布局，更改取消和转文字效果更加方便用户滑动。
3、如果用户选择转文字，那么实现将语言转换成文字，并且直接发送出去（需要判断未读取到文字的情况，给予提醒，并中断发送逻辑）。
4、设置变量控制，是否长按语音松手后的逻辑为直接发送语音，如果为false，那么长按之后默认转文字后发送,并且长按时候的蒙板样式只保留录音效果和上滑取消，如果设置为true，那么蒙板样式布局为取消、转文字、录音效果操作，并且默认松手为发送语音信息效果。
5、文件类型和图片消息参考图二，不使用默认的聊天气泡盒子，文件单独写一个组件，样式参考图二，图片根据原图等比例充满盒子不裁剪的模式显示。
